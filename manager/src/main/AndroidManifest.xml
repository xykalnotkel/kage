<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Kage's own API permission: client apps request this, the server grants it through the shell -->
    <permission
        android:name="dev.kage.permission.API"
        android:label="@string/api_permission_label"
        android:description="@string/api_permission_description"
        android:protectionLevel="dangerous" />
    <uses-permission
        android:name="dev.kage.permission.API"
        tools:node="remove" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- third party apps open this to ask the user for the Kage permission -->
        <activity
            android:name=".ui.RequestPermissionActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/request_permission_title"
            android:theme="@style/AppTheme.Dialog">
            <intent-filter>
                <action android:name="dev.kage.manager.intent.action.REQUEST_PERMISSION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="@string/setup_title" />

        <activity
            android:name=".ui.LogsActivity"
            android:exported="false"
            android:label="@string/logs_title" />

        <provider
            android:name="dev.kage.provider.KageProvider"
            android:authorities="dev.kage.manager.kage"
            android:exported="true" />
    </application>
</manifest>
