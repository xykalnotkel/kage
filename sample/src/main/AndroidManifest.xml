<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- izin yang dikelola manager Kage (bukan izin Android biasa) -->
    <uses-permission android:name="dev.kage.permission.API" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/SampleTheme">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WAJIB: provider ini yang menerima Binder dari server Kage -->
        <provider
            android:name="dev.kage.provider.KageProvider"
            android:authorities="${applicationId}.kage"
            android:exported="true" />
    </application>
</manifest>
